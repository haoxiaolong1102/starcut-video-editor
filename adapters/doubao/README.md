# Doubao adapter

No one-click Agent Skill import is claimed. Use the current custom Agent/Skills/work-task functions only where the installed product supports them.

- Use `../generic-agent/MASTER_PROMPT.md` as the core custom instruction.
- Add the workflow and relevant references as allowed knowledge files.
- Treat local video access, screen control, image generation, and rendering as separate capabilities that must be explicitly available and authorized.
- Missing local execution means StarCut outputs planning artifacts, not a falsely reported finished render.
- Do not upload private RAW media to a cloud product without user authorization.
